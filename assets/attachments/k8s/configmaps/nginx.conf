user nginx;
worker_processes 4;
pid /run/nginx.pid;
events {
   worker_connections 768;
}
http {
  server {
    listen *:80;
    # Route /whoami vers le service Kubernetes "whoami" (resolu par le DNS interne)
    location = /whoami {
      proxy_pass http://whoami/;
    }
  }
}
