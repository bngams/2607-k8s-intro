# Secrets / credentials — ne JAMAIS committer
*.env
.env
secrets/
*.key
*.pem
kubeconfig
argocd-*.secret
mongo_url            # généré par le lab 05 (--from-file), contient des credentials

# Tokens
*token*
*.tfvars
!*.tfvars.example

# OS / éditeur
.DS_Store
