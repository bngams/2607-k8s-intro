apiVersion: v1
kind: ResourceQuota
metadata:
  name: whoami-quota
  namespace: whoami
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "5"
    services: "3"
    count/deployments.apps: "2"
